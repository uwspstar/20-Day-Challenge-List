### Common Docker Commands and Their Usage

| **Command**                                  | **Description**                                                                                                                                           | **Usage Example**                                                                                                                                              | **中文翻译**                                |
|----------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------|---------------------------------------------|
| `docker --version`                           | Displays the installed Docker version.                                                                                                                    | ```docker --version```                                                                                                                                        | 显示安装的 Docker 版本。                    |
| `docker ps`                                  | Lists all running containers.                                                                                                                             | ```docker ps```                                                                                                                                               | 列出所有正在运行的容器。                    |
| `docker ps -a`                               | Lists all containers, including those that are stopped.                                                                                                   | ```docker ps -a```                                                                                                                                            | 列出所有容器，包括已停止的容器。             |
| `docker images`                              | Lists all Docker images available on your system.                                                                                                         | ```docker images```                                                                                                                                           | 列出系统上可用的所有 Docker 镜像。           |
| `docker build -t <image_name> .`             | Builds a Docker image from a Dockerfile in the current directory and tags it with the provided name.                                                      | ```docker build -t myapp .```                                                                                                                                 | 从当前目录中的 Dockerfile 构建 Docker 镜像，并使用提供的名称对其进行标记。 |
| `docker run --rm -it <image_name>`           | Runs a container from the specified image and removes the container after it exits. The `-it` flag makes the container interactive.                       | ```docker run --rm -it myapp```                                                                                                                               | 从指定的镜像运行容器，并在其退出后删除容器。       |
| `docker exec -it <container_id> /bin/bash`   | Opens an interactive bash shell inside a running container.                                                                                               | ```docker exec -it 123abc456 /bin/bash```                                                                                                                     | 在运行的容器中打开交互式 bash shell。         |
| `docker stop <container_id>`                 | Stops a running container.                                                                                                                                | ```docker stop 123abc456```                                                                                                                                   | 停止正在运行的容器。                          |
| `docker rm <container_id>`                   | Removes a stopped container.                                                                                                                              | ```docker rm 123abc456```                                                                                                                                     | 删除已停止的容器。                            |
| `docker rmi <image_id>`                      | Removes an image from your system.                                                                                                                        | ```docker rmi 789def012```                                                                                                                                   | 从系统中删除镜像。                            |
| `docker pull <image_name>`                   | Pulls a Docker image from a registry (like Docker Hub).                                                                                                   | ```docker pull nginx```                                                                                                                                       | 从注册表（如 Docker Hub）拉取 Docker 镜像。    |
| `docker push <image_name>`                   | Pushes a Docker image to a registry (like Docker Hub).                                                                                                    | ```docker push myapp```                                                                                                                                       | 将 Docker 镜像推送到注册表（如 Docker Hub）。   |
| `docker logs <container_id>`                 | Displays the logs from a running or stopped container.                                                                                                    | ```docker logs 123abc456```                                                                                                                                   | 显示正在运行或已停止容器的日志。               |
| `docker-compose up`                          | Builds, (re)creates, starts, and attaches to containers for a service defined in a `docker-compose.yml` file.                                             | ```docker-compose up```                                                                                                                                       | 根据 `docker-compose.yml` 文件定义的服务构建、重新创建、启动并附加到容器。 |
| `docker-compose down`                        | Stops and removes containers, networks, images, and volumes created by `docker-compose up`.                                                               | ```docker-compose down```                                                                                                                                     | 停止并删除由 `docker-compose up` 创建的容器、网络、镜像和卷。 |



This guide should help you understand how to use Docker commands effectively within Docker Desktop, whether through the system terminal or the integrated terminal interface.

------

Here is a table that summarizes the provided Docker command information, including explanations, code examples, warnings, and tips:

| **Category**          | **Command** | **Explanation**                                                                 | **Code Example**                                     | **Warnings**                                                                                               | **Tips**                                                                                                   |
|-----------------------|-------------|---------------------------------------------------------------------------------|------------------------------------------------------|------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------|
| **Common Commands**   | `run`       | Create and run a new container from an image.                                    | `docker run -d -p 80:80 nginx`                        | Ensure the image exists locally or in the registry.                                                        | Use `-d` to run the container in detached mode, and `-p` to map ports.                                     |
|                       | `exec`      | Execute a command in a running container.                                        | `docker exec -it container_name /bin/bash`            | Make sure the container is running before executing commands.                                              | Use `-it` for interactive terminal access.                                                                 |
|                       | `ps`        | List all running containers.                                                     | `docker ps`                                           | -                                                                                                          | Use `docker ps -a` to see all containers, including stopped ones.                                          |
|                       | `build`     | Build an image from a Dockerfile.                                                | `docker build -t myimage:latest .`                    | Ensure Dockerfile is in the correct directory.                                                             | Use `.dockerignore` to exclude files from the build context.                                               |
|                       | `pull`      | Download an image from a registry.                                               | `docker pull ubuntu:latest`                           | Ensure the image name and tag are correct.                                                                 | Pull specific versions of images to avoid compatibility issues.                                            |
|                       | `push`      | Upload an image to a registry.                                                   | `docker push myimage:latest`                          | You need to be logged in to the registry.                                                                  | Use `docker tag` to rename images before pushing.                                                          |
|                       | `images`    | List all images on the local system.                                             | `docker images`                                       | -                                                                                                          | Use `docker images -q` to list only image IDs.                                                             |
|                       | `login`     | Log in to a Docker registry.                                                     | `docker login`                                        | Ensure you have the correct credentials.                                                                   | Use environment variables to securely manage credentials.                                                  |
|                       | `logout`    | Log out from a Docker registry.                                                  | `docker logout`                                       | -                                                                                                          | -                                                                                                          |
|                       | `search`    | Search Docker Hub for images.                                                    | `docker search nginx`                                 | Ensure network connectivity.                                                                               | Use filtering options to narrow down search results.                                                       |
|                       | `version`   | Show Docker version information.                                                 | `docker version`                                      | -                                                                                                          | Use `docker info` for more detailed system information.                                                    |
|                       | `info`      | Display system-wide Docker information.                                          | `docker info`                                         | -                                                                                                          | Useful for troubleshooting system-related issues.                                                          |
| **Management Commands** | `builder`    | Manage builds and related configurations.                                        | `docker builder prune`                                | Be cautious with pruning commands; they may remove valuable cached data.                                   | Use `docker builder inspect` to see details about the builder.                                             |
|                       | `buildx`*   | Docker Buildx, an advanced build tool.                                           | `docker buildx create --name mybuilder`               | Experimental feature, ensure it's enabled in your Docker setup.                                            | Use for multi-platform builds.                                                                             |
|                       | `compose`*  | Docker Compose, manage multi-container Docker applications.                      | `docker-compose up`                                   | Ensure `docker-compose.yml` is properly configured.                                                        | Use `docker-compose -f <file>` to specify an alternate Compose file.                                       |
|                       | `container` | Manage containers (start, stop, restart, etc.).                                  | `docker container ls`                                 | -                                                                                                          | Use `docker container prune` to clean up stopped containers.                                               |
|                       | `context`   | Manage contexts for connecting to different Docker environments.                 | `docker context create mycontext`                     | Incorrect context configuration may lead to connection issues.                                             | Switch contexts with `docker context use mycontext`.                                                       |
|                       | `debug`*    | Get a shell into any image or container for debugging.                           | `docker debug --image myimage`                        | Experimental feature, may not be available in all Docker versions.                                         | Useful for troubleshooting Dockerfiles and containers.                                                     |
|                       | `desktop`*  | Docker Desktop commands (Alpha).                                                 | `docker desktop restart`                              | May be unstable, especially in Alpha stage.                                                                | -                                                                                                          |
|                       | `dev`*      | Docker Dev Environments, for managing development environments.                  | `docker dev create`                                   | Experimental feature, ensure it's enabled.                                                                 | -                                                                                                          |
|                       | `extension`*| Manage Docker extensions.                                                        | `docker extension install <name>`                     | -                                                                                                          | Explore available extensions on Docker Hub.                                                                |
|                       | `feedback`* | Provide feedback from the terminal.                                              | `docker feedback`                                     | -                                                                                                          | -                                                                                                          |
|                       | `image`     | Manage Docker images (inspect, remove, etc.).                                    | `docker image inspect myimage`                        | Use caution with `docker image rm` as it permanently removes images.                                       | Use `docker image prune` to remove unused images.                                                          |
|                       | `init`*     | Creates Docker-related starter files for your project.                           | `docker init`                                         | Experimental feature, ensure it's enabled.                                                                 | Useful for initializing new projects.                                                                      |
|                       | `manifest`  | Manage Docker image manifests and manifest lists.                                | `docker manifest create`                              | -                                                                                                          | Use for creating multi-platform images.                                                                    |
|                       | `network`   | Manage Docker networks.                                                          | `docker network ls`                                   | -                                                                                                          | Use `docker network prune` to clean up unused networks.                                                    |
|                       | `plugin`    | Manage Docker plugins.                                                           | `docker plugin install`                               | Ensure plugins are compatible with your Docker version.                                                    | Use `docker plugin ls` to list installed plugins.                                                          |
|                       | `sbom`*     | View the packaged-based Software Bill Of Materials (SBOM) for an image.          | `docker sbom myimage`                                 | -                                                                                                          | Useful for security and compliance checks.                                                                 |
|                       | `scout`*    | Docker Scout, a tool for managing container security and compliance.             | `docker scout cves`                                   | -                                                                                                          | Use to scan for vulnerabilities.                                                                           |
|                       | `system`    | Manage Docker system (events, df, etc.).                                         | `docker system df`                                    | Be cautious with pruning commands; they can remove valuable data.                                          | Use `docker system prune` to clean up unused resources.                                                    |
|                       | `trust`     | Manage trust on Docker images.                                                   | `docker trust sign myimage`                           | Ensure your Docker setup supports Notary.                                                                  | Use for secure image signing.                                                                              |
|                       | `volume`    | Manage Docker volumes.                                                           | `docker volume ls`                                    | Be cautious when removing volumes as they may contain important data.                                      | Use `docker volume prune` to remove unused volumes.                                                        |
| **Swarm Commands**    | `swarm`     | Manage Docker Swarm, a clustering and scheduling tool for containers.            | `docker swarm init`                                   | Ensure Docker Swarm mode is enabled before running commands.                                               | Use `docker swarm join` to add nodes to your swarm cluster.                                                |
| **Commands**          | `attach`    | Attach to a running container’s input/output.                                    | `docker attach container_name`                        | Detaching from a container may stop it if not handled correctly.                                           | Use `Ctrl + p` then `Ctrl + q` to detach without stopping the container.                                   |
|                       | `commit`    | Create a new image from a container’s changes.                                   | `docker commit container_name mynewimage`             | Make sure to clean up unnecessary files before committing to avoid bloated images.                         | Use descriptive commit messages to document changes.                                                       |
|                       | `cp`        | Copy files/folders between a container and the local filesystem.                 | `docker cp container_name:/path/to/file /local/path`  | Be aware of permissions issues when copying files.                                                         | Use `docker cp -a` to preserve the original file attributes.                                               |
|                       | `create`    | Create a new container without starting it.                                      | `docker create --name mycontainer nginx`              | -                                                                                                          | Use `docker start` to start the container after creation.                                                  |
|                       | `diff`      | Inspect changes to files or directories on a container’s filesystem.             | `docker diff container_name`                          | -                                                                                                          | Useful for debugging container issues.                                                                     |
|                       | `events`    | Get real-time events from the Docker server.                                     | `docker events`                                       | Ensure you have appropriate permissions to monitor Docker events.                                          | Useful for monitoring and logging Docker activities.                                                       |
|                       | `export`    | Export a container’s filesystem as a tar archive.                                | `docker export container_name > mycontainer.tar`      | This command does not include the container's layers and metadata.                                         | Use `docker save` for a more complete export, including metadata.                                          |
|                       | `history`   | Show the history of an image.                                                    | `docker history myimage`                              | -                                                                                                          | Useful for auditing image creation and

 changes.                                                            |
|                       | `import`    | Import the contents from a tarball to create a filesystem image.                 | `docker import mycontainer.tar mynewimage`            | -                                                                                                          | -                                                                                                          |
|                       | `inspect`   | Return low-level information on Docker objects.                                  | `docker inspect container_name`                       | -                                                                                                          | Use `jq` to parse the JSON output for easier reading.                                                      |
|                       | `kill`      | Kill one or more running containers.                                             | `docker kill container_name`                          | Use with caution as it immediately stops the container without cleanup.                                     | Use `docker stop` for a graceful shutdown if possible.                                                     |
|                       | `load`      | Load an image from a tar archive or STDIN.                                       | `docker load < myimage.tar`                           | Ensure the image file is correctly formatted.                                                              | Use `docker save` for creating archives compatible with `docker load`.                                     |
|                       | `logs`      | Fetch the logs of a container.                                                   | `docker logs container_name`                          | Logs can grow large; ensure proper management of log sizes.                                                | Use `docker logs -f` to follow the logs in real-time.                                                      |
|                       | `pause`     | Pause all processes within one or more containers.                               | `docker pause container_name`                         | Paused containers still consume resources.                                                                 | Use `docker unpause` to resume the container’s processes.                                                  |
|                       | `port`      | List port mappings or a specific mapping for the container.                      | `docker port container_name`                          | -                                                                                                          | Use to verify port mappings.                                                                               |
|                       | `rename`    | Rename a container.                                                              | `docker rename old_name new_name`                     | Ensure the new name is not already in use.                                                                 | Use descriptive names for containers for better management.                                                |
|                       | `restart`   | Restart one or more containers.                                                  | `docker restart container_name`                       | -                                                                                                          | Useful for applying new configurations or updates.                                                         |
|                       | `rm`        | Remove one or more containers.                                                   | `docker rm container_name`                            | Removing a running container will stop it; use `docker stop` before `rm`.                                  | Use `docker rm -f` to force remove running containers (use with caution).                                  |
|                       | `rmi`       | Remove one or more images.                                                       | `docker rmi myimage`                                  | Ensure the image is not in use by any containers.                                                          | Use `docker images -q` to get the image ID for easier removal.                                             |
|                       | `save`      | Save one or more images to a tar archive.                                        | `docker save -o myimage.tar myimage`                  | Ensure enough disk space is available for large images.                                                    | Use `docker load` to reload the image from the archive.                                                    |
|                       | `start`     | Start one or more stopped containers.                                            | `docker start container_name`                         | -                                                                                                          | Use `docker ps -a` to list all containers, including stopped ones.                                         |
|                       | `stats`     | Display a live stream of container(s) resource usage statistics.                 | `docker stats container_name`                         | High resource usage in a container can affect the host system.                                             | Use for monitoring container performance in real-time.                                                     |
|                       | `stop`      | Stop one or more running containers.                                             | `docker stop container_name`                          | Ensure you have saved necessary data before stopping the container.                                        | Use `docker kill` if the container does not stop gracefully.                                               |
|                       | `tag`       | Create a tag that refers to a source image.                                      | `docker tag source_image:tag target_image:tag`        | Ensure the target image name follows the correct naming convention.                                        | Use descriptive tags for easier management of image versions.                                              |
|                       | `top`       | Display the running processes of a container.                                    | `docker top container_name`                           | -                                                                                                          | Useful for monitoring what processes are running inside a container.                                       |
|                       | `unpause`   | Unpause all processes within one or more containers.                             | `docker unpause container_name`                       | Ensure the container was intentionally paused before unpausing.                                            | -                                                                                                          |
|                       | `update`    | Update the configuration of one or more containers.                              | `docker update container_name --cpu-shares 512`       | Be cautious when changing configurations as it can affect container performance.                           | Use for live adjustments to running containers.                                                            |
|                       | `wait`      | Block until one or more containers stop, then print their exit codes.            | `docker wait container_name`                          | The command will block until the container stops, which may be indefinite.                                 | Use for automation scripts that need to wait for a container to finish.                                    |
| **Global Options**    | `--config`  | Set the location of client config files.                                         | `docker --config /my/config/path`                     | Ensure the config path is correct to avoid misconfigurations.                                              | Use for customizing Docker configurations for different environments.                                      |
|                       | `--context` | Specify the context to use for connecting to Docker.                             | `docker --context mycontext ps`                       | Incorrect context may lead to connection issues.                                                           | Use `docker context use <context>` to switch contexts easily.                                              |
|                       | `--debug`   | Enable debug mode for more detailed output.                                      | `docker --debug run myimage`                          | Debug mode may expose sensitive information in logs.                                                       | Useful for troubleshooting Docker commands and configurations.                                             |
|                       | `--host`    | Specify the Docker daemon socket to connect to.                                  | `docker --host tcp://0.0.0.0:2376 ps`                 | Ensure the specified host is correctly configured and accessible.                                          | Useful for managing Docker daemons on remote servers.                                                      |
|                       | `--log-level`| Set the logging level (e.g., debug, info, warn, error, fatal).                   | `docker --log-level debug run myimage`                | Using a high verbosity log level like `debug` may generate large log files.                                | Use appropriate log levels depending on the environment (e.g., debug for development, info for production).|
|                       | `--tls`     | Use TLS for Docker daemon connections.                                           | `docker --tls --tlscacert /my/ca.pem ps`              | Ensure TLS certificates are correctly configured and secure.                                               | Use for secure communications with Docker daemons.                                                         |
|                       | `--version` | Print version information and quit.                                              | `docker --version`                                    | -                                                                                                          | Use to check the Docker version installed on your system.                                                  |

*Commands marked with an asterisk (*) are experimental and may not be available or stable in all Docker versions.

This table organizes the commands, explanations, code examples, warnings, and tips to help you understand and effectively use Docker.
